<h1>Articles</h1>

Our blog has <%= Article.public_count %> public articles and <%= Article.private_count %> private articles and counting!

<%= form_tag(articles_path, method: "get") do %>
    <%= text_field_tag :search, params[:search], placeholder: "Search articles by title or author's name" %>
    <%= submit_tag "Search" %>
<% end %>

<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
        <li>
            <%= link_to article.title, article %>
            by: 
            <% if article.user.present? %>
            <p>Author: <%= article.user.name %></p>
          <% else %>
            <p>Author: Unknown</p>
          <% end %>
            
        </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>